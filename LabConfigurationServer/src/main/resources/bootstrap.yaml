server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: file://C:\work\MSTraining\PuneDec10\labconfigs

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: ALWAYS
          